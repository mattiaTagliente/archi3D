# Archi3D Configuration
# Copy this file to .env and update with your settings.
# The .env file is gitignored and will not be committed.

# REQUIRED: Absolute path to the workspace directory
# Use forward slashes (/) even on Windows for compatibility
ARCHI3D_WORKSPACE="C:/Users/yourname/path/to/Testing"

# Optional: force worker identity (defaults to OS username)
# ARCHI3D_WORKER_ID=yourname
